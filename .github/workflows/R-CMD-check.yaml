on:
  workflow_dispatch:

name: R-CMD-check

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest,   r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.AGNE_PAT }}   # exported for every step
      R_KEEP_PKG_SOURCE: yes

    steps:
      # --------------------------------------------------------------
      # 0️⃣ Checkout the repository
      # --------------------------------------------------------------
      - uses: actions/checkout@v5

      # --------------------------------------------------------------
      # 1️⃣ Install OS‑level libcurl (Ubuntu) / curl (Windows)
      # --------------------------------------------------------------
      - name: Install libcurl on Ubuntu
        if: matrix.config.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
        shell: bash

      - name: Install curl on Windows via Chocolatey
        if: matrix.config.os == 'windows-latest'
        run: |
          choco install -y curl
        shell: powershell

      # --------------------------------------------------------------
      # 2️⃣ Re‑export the PAT (makes sure it survives any caching step)
      # --------------------------------------------------------------
      - name: Export GitHub PAT for R
        run: echo "GITHUB_PAT=${{ secrets.AGNE_PAT }}" >> $GITHUB_ENV

      # --------------------------------------------------------------
      # 3️⃣ Set CRAN mirror for the whole job (works on Linux & Windows)
      # --------------------------------------------------------------
      - name: Set CRAN mirror via R_REPOS
        run: |
          echo "R_REPOS=https://cloud.r-project.org" >> $GITHUB_ENV
        shell: bash

      # --------------------------------------------------------------
      # 4️⃣ Install pandoc (required by many R packages)
      # --------------------------------------------------------------
      - uses: r-lib/actions/setup-pandoc@v2

      # --------------------------------------------------------------
      # 5️⃣ Install the requested R version (public RSPM)
      # --------------------------------------------------------------
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          use-public-rspm: true

      # --------------------------------------------------------------
      # 6️⃣ **ONE** step that (a) removes old pak, (b) installs newest pak,
      #     (c) sets token options, (d) runs diagnostics & install.
      # --------------------------------------------------------------
      - name: Install latest *development* pak and resolve dependencies
        env:
          GITHUB_PAT: ${{ secrets.AGNE_PAT }}   # token for GitHub API
        run: |
          # --------------------------------------------------------------
          # 0️⃣  Wipe any existing copy of pak from *all* library paths
          # --------------------------------------------------------------
          lib_paths <- .libPaths()
          for (lp in lib_paths) {
            pak_dir <- file.path(lp, "pak")
            if (dir.exists(pak_dir)) {
              unlink(pak_dir, recursive = TRUE, force = TRUE)
              cat("🗑️ Removed stale pak from", lp, "\n")
            }
          }
      
          # --------------------------------------------------------------
          # 1️⃣  Install the *development* version of pak from GitHub
          # --------------------------------------------------------------
          if (!requireNamespace("remotes", quietly = TRUE)) install.packages("remotes")
          remotes::install_github(
            repo = "r-lib/pak",
            ref  = "main",   # ← name of the branch that defines pkg_install2()
            force = TRUE,                    # force a fresh download (no cached tarball)
            upgrade = "always"               # rebuild any compiled bits
          )   # pulls the latest dev commit
          pak_ver <- as.character(packageVersion("pak"))
          cat("📦 pak development version installed:", pak_ver, "\n")

          install_with_ignore <- function(project_root = ".",
                                          ignore = character(),
                                          deps = TRUE) {
            # Resolve the full dependency graph for the project
            dep_df <- pak::pkg_deps(project_root, dependencies = deps)
            all_pkgs <- unique(dep_df$package)

            # Drop the packages we want to ignore
            to_install <- setdiff(all_pkgs, ignore)

            cat("\n🔎 Packages to install (ignoring:",
                paste(ignore, collapse = ", "), ")\n")
            cat(paste(to_install, collapse = "\n"), "\n\n")

            # Install each remaining package, catching errors so the loop continues
            for (pkg in to_install) {
              cat("🚀 Installing:", pkg, "\n")
              tryCatch(
                pkg_install(pkg, dependencies = FALSE),
                error = function(e) {
                  cat("⚠️  Skipping", pkg,
                      "- error:", conditionMessage(e), "\n")
                }
              )
            }
            cat("\n✅ Finished (ignored:",
                paste(ignore, collapse = ", "), ")\n")
          }
      
          # --------------------------------------------------------------
          # 2️⃣  Make the token visible to every possible code path
          # --------------------------------------------------------------
          Sys.setenv(GITHUB_TOKEN = Sys.getenv("GITHUB_PAT"))   # legacy name
          options(pak.github_pat = Sys.getenv("GITHUB_PAT"))   # new name
      
          # --------------------------------------------------------------
          # 3️⃣  (Optional) sanity‑check that GitHub is reachable
          # --------------------------------------------------------------
          if (!requireNamespace("gh", quietly = TRUE)) install.packages("gh")
          tryCatch(
            gh::gh("/repos/KTH-Library/kthcorpus"),
            error = function(e) {
              cat("⚠️ GitHub test failed – you may still be offline or token missing\n")
              cat("Error:", conditionMessage(e), "\n")
            }
          )
      
          # --------------------------------------------------------------
          # 4️⃣  Dependency diagnostics (helps spot version‑range conflicts)
          # --------------------------------------------------------------
          tryCatch({
            pak::pkg_deps(".", dependencies = TRUE, conflict = "verbose")
          }, error = function(e) {
            cat("🔎 Diagnostics finished – see above for details.\n")
          })
      
          # --------------------------------------------------------------
          # 5️⃣  Normal install – should now succeed (token works, fresh pak loaded)
          # --------------------------------------------------------------
          normal_ok <- tryCatch({
            install_with_ignore(".",
                                ignore = c("kthcorpus", "minioclient"),
                                deps = TRUE)
            TRUE
          }, error = function(e) {
            cat("❌ Normal install failed:", conditionMessage(e), "\n")
            FALSE
          })
      
          # --------------------------------------------------------------
          # 6️⃣  Fallback – ignore the two GitHub‑only packages if they still fail
          # --------------------------------------------------------------
          if (!normal_ok) {
            ignore_pkgs <- c("kthcorpus", "minioclient")   # **package names**, not repo URLs
            cat("🚧 Falling back – ignoring:", paste(ignore_pkgs, collapse = ", "), "\n")
            install_with_ignore(".", deps = TRUE, ignore = ignore_pkgs)
          }
      
          cat("\n✅ Dependency resolution finished\n")
        shell: Rscript {0}
  
      # --------------------------------------------------------------
      # 7️⃣ Run the usual R CMD check
      # --------------------------------------------------------------
      - uses: r-lib/actions/check-r-package@v2
